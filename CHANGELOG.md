# Changelog

Append-only, project-wide version history.

No version has been cut yet. Per the versioning guardrail (`GOVERNANCE.md` item 2), a version number
only moves once the artifacts behind it actually exist and have been piloted/validated against real
Azure — never on a calendar date or to reflect intent.

## Unreleased

- 2026-08-20: Host pool converted Pooled → Personal, NAT Gateway added for outbound internet, Office
  Apps post-install extension added (Sausage topping only), Pickup/Delivery networking picker UI
  added (Delivery non-functional, targets 2026-09-29). See RAID Decisions `D-3`/`D-4`/`D-5` and Scope
  Discovery `SD-3` for full reasoning. Not yet deployed/validated against real Azure (`ASM-1`).
- 2026-08-20: `GOVERNANCE.md`, this file, and `CLAUDE.md`'s Core Session Rules section initialized —
  project moved from minimal onboarding to normal governance once it became public-facing and shared
  with teammates for review.
